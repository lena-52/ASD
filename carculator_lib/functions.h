#pragma once
#include <cmath>

namespace Functions {
    double my_sin(double x);
    double my_cos(double x);
    double my_tg(double x);    // через sin/cos
    double my_abs(double x);
    double my_pow(double base, double exp);
}